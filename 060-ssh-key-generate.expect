#!/usr/bin/expect
set timeout 20
spawn "/usr/bin/ssh-keygen"
expect ".ssh/id_rsa):"	 		{ send "\r" }
expect "empty for no passphrase):" 	{ send "\r" }
expect "same passphrase again:" 	{ send "\r" }
interact
# expect "verwrite (y/n)?"	{ send "y\r" }
